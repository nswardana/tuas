/// <reference types="react" />
import { BoxProps } from "@material-ui/core/Box";
export declare const flexClasses: Record<"col" | "row" | "item", string>;
export interface ItemProps extends BoxProps {
    grow?: true | "stretched";
    noShrink?: true;
}
export declare const Item: ({ className, noShrink, grow, ...props }: ItemProps) => JSX.Element;
export interface FlexRowProps extends ItemProps {
    /**
     * the content width (not include padding and border) that will cause the layout to shift to stack
     */
    stackPoint?: string | number;
    noWrap?: true;
}
export declare const FlexRow: ({ className, gap, noWrap, stackPoint, ...props }: FlexRowProps) => JSX.Element;
export declare const FlexCol: ({ className, gap, ...props }: ItemProps) => JSX.Element;
export declare const Flex: {
    Row: ({ className, gap, noWrap, stackPoint, ...props }: FlexRowProps) => JSX.Element;
    Col: ({ className, gap, ...props }: ItemProps) => JSX.Element;
    Item: ({ className, noShrink, grow, ...props }: ItemProps) => JSX.Element;
};
