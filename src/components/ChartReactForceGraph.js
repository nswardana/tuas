import React, { Fragment } from 'react'
import {
  InteractiveForceGraph,
  ForceGraphNode,
  ForceGraphLink,
} from 'react-vis-force'

export default function ChartForceGraph() {
  return <Fragment></Fragment>
}
